# Copyright (c) 2026 Sergey Yakunin <sergey@squidy.ru>

name: Build

on:
  push:
    branches:
      - '**'
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: build-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  QT_VERSION: '6.10.2'
  AQT_WINDOWS_SOURCE: 'git+https://github.com/miurahr/aqtinstall.git@9e49c82edc6d946db376dec907cca5b4b486eec5'

jobs:
  build:
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Linux
            os: ubuntu-latest
          - name: Windows
            os: windows-latest
          - name: macOS
            os: macos-15

    steps:
      - name: Check out repository
        uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: ${{ env.QT_VERSION }}
          aqtsource: ${{ runner.os == 'Windows' && env.AQT_WINDOWS_SOURCE || '' }}
          cache: true

      - name: Install Inno Setup
        if: runner.os == 'Windows'
        shell: pwsh
        run: choco upgrade innosetup --yes --no-progress

      - name: Configure
        run: cmake -S . -B build -DCMAKE_BUILD_TYPE=Release -DCMAKE_INSTALL_PREFIX="${{ github.workspace }}/install"

      - name: Build
        run: cmake --build build --config Release --parallel

      - name: Test
        id: test
        run: ctest --test-dir build --build-config Release --output-on-failure

      # ctest hands back an empty log for the widget suite when it fails on
      # Windows, so it is run again here with nothing in between to swallow
      # what it prints.
      - name: Report the widget suite
        if: failure() && runner.os == 'Windows' && steps.test.outcome == 'failure'
        continue-on-error: true
        env:
          QT_QPA_PLATFORM: offscreen
        shell: pwsh
        run: '& .\build\tests\Release\tst_repositoryview.exe'

      - name: Verify installation
        run: cmake --install build --config Release

      - name: Verify Windows installer
        if: runner.os == 'Windows'
        run: cmake --build build --config Release --target windows_installer
